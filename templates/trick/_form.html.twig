
    <div class="row mt-4 ">
        <div class="col-6 mx-4">
            {% form_theme form 'bootstrap_5_layout.html.twig' %}
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary m-4">{{ button_label|default('Envoyer') }}</button>
            {{ form_end(form) }}
        </div>
    </div>
